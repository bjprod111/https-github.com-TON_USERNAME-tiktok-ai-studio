{
  "name": "clipflow-ai",
  "version": "0.2.0",
  "private": true,
  "description": "A privacy-first short-form content planning workspace with a deterministic template engine.",
  "type": "module",
  "scripts": {
    "build": "mkdir -p build && cp index.html build/index.html && cp styles.css build/styles.css && cp app.js build/app.js",
    "test": "node --test tests/planner.test.js"
  },
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  }
}
